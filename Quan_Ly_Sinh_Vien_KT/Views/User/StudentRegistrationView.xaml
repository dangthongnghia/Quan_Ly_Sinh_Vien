<!-- StudentRegistrationView.xaml -->
<Window x:Class="Quan_Ly_Sinh_Vien_KT.Views.User.StudentRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quan_Ly_Sinh_Vien_KT"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ĐĂNG KÝ MÔN HỌC SINH VIÊN" 
                   FontSize="20" FontWeight="Bold" Margin="0,10,0,20"/>

        <!-- Filter options -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Sinh viên:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox Grid.Column="1" x:Name="cbStudentFilter" Margin="5" Padding="5,3"/>
            <Button Grid.Column="2" x:Name="btnSearch" Content="Tìm kiếm" Margin="5"
                    Width="100" Height="30" Background="#2196F3" Foreground="White"/>
            <Button Grid.Column="3" x:Name="btnExport" Content="Xuất Excel" Margin="5"
                    Width="100" Height="30" Background="#4CAF50" Foreground="White"/>
        </Grid>

        <!-- Data grid for displaying student registrations -->
        <DataGrid Grid.Row="2" x:Name="dgRegistrations" AutoGenerateColumns="False" 
                  IsReadOnly="True" AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentId}" Width="100"/>
                <DataGridTextColumn Header="Họ và tên" Binding="{Binding StudentName}" Width="150"/>
                <DataGridTextColumn Header="Mã môn học" Binding="{Binding SubjectId}" Width="100"/>
                <DataGridTextColumn Header="Tên môn học" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Số tín chỉ" Binding="{Binding Credits}" Width="80"/>
                <DataGridTextColumn Header="Ngày đăng ký" Binding="{Binding RegistrationDate, StringFormat=dd/MM/yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>